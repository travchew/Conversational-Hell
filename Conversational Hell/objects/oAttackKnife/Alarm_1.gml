/// @description stop spinning, shoot

direction = point_direction(x,y, oP.x, oP.y);

speed = my_speed;

if (sprite_index = knife_big) {
	audio_play_sound(snd_shakerbreaker, 10, false);
}
else {
	audio_play_sound(snd_butcher, 10, false);
}
